<% sidebar do %>
  <h2>Events</h2>
  <ul>
    <%= render @events %>
  </ul>
<% end %>

<% @events.each do |event| %>
  <% if videos = event.videos.presence %>
    <h3><%= link_to event.name, event %></h3>
    <ul class="media-grid">
      <%= render videos %>
    </ul>
  <% end %>
<% end %>
