<% sidebar do %>
  <h2>Events</h2>
  <ul>
    <%= render @events %>
  </ul>
<% end %>

<% @events.each do |event| %>
  <h3><%= link_to event.name, event %></h3>
  <ul class="media-grid">
    <%= render event.photos %>
  </ul>
<% end %>
