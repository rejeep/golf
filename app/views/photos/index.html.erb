<% sidebar do %>
  <h2>Events</h2>
  <ul>
    <%= render @events %>
  </ul>
<% end %>

<% @events.each do |event| %>
  <% if photos = event.photos.presence %>
    <h3><%= link_to event.name, event %></h3>
    <ul class="media-grid">
      <%= render photos %>
    </ul>
  <% end %>
<% end %>
