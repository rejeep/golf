<h2>User information</h2>

<%= simple_form_for current_user, :html => { :class => 'form-stacked' } do |form| %>
  <%= form.input :first_name %>
  <%= form.input :last_name %>
  
  <%= form.submit :class => 'btn' %>
<% end %>
