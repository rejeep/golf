<div class="span7">
  <h2><%= link_to 'Videos', admin_videos_path %></h2>
  <ul class="media-grid">
    <% @videos.each do |video| %>
      <li>
        <%= link_to image_tag('video.jpg'), edit_admin_video_path(video) %>
      </li>
    <% end %>
  </ul>
</div>
