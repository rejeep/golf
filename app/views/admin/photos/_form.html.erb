<%= simple_form_for [:admin, @photo], :html => { :multipart => true, :class => 'form-stacked' } do |form| %>
  <% if @photo.image.present? %>
    <%= image_tag @photo.image.url(:thumb) %>
  <% end %>
  <%= form.input :image, :as => :file %>
  <%= form.input :description %>
  <%= form.input :tag_ids %>
  
  <%= form.submit :class => 'btn' %>
<% end %>
