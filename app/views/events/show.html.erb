<h2><%= @event.name %></h2>

<% if photos = @event.photos.presence %>
  <h3>Photos</h3>
  <ul class="media-grid">
    <%= render photos %>
  </ul>
<% end %>

<% if videos = @event.videos.presence %>
  <h3>Videos</h3>
  <ul class="media-grid">
    <%= render videos %>
  </ul>
<% end %>
